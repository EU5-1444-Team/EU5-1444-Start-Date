namespace = 1444_flavor_bosnia

1444_flavor_bosnia.1 = {
	type = country_event
	title = 1444_flavor_bosnia.1.title
	desc = 1444_flavor_bosnia.1.desc

	fire_only_once = yes
	dynamic_historical_event = {
		tag = BOS
		from = 1444.1.1
		to = 1450.1.1
		monthly_chance = 100
	}

	trigger = {
		ruler ?= character:bos_thomas_kotromanic
		ruler = {
			first_spouse = character:bos_vojaca
		}
		character:hum_catherine_of_bosnia ?= {
			is_alive = yes
			has_available_marriage_slot = yes
		}
	}

	immediate = {
		event_illustration_estate_effect = {
			foreground = estate_type:nobles_estate
			background = estate_type:clergy_estate
		}
		character:hum_catherine_of_bosnia ?= {
			save_scope_as = target_catherine
		}
	}

	illustration_tags = {
		10 = regular
		10 = interior
	}

	option = {
		name = 1444_flavor_bosnia.1.a
		historical_option = yes

		ruler = {
			divorce_character = first_spouse
			scope:target_catherine = { move_country = root }
			marry_character = scope:target_catherine
		}

		scope:target_catherine = {
			change_character_religion = religion:catholic
		}
	}

	option = {
		name = 1444_flavor_bosnia.1.b
	}
}

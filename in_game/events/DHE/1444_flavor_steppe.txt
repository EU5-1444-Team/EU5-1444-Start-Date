namespace = flavor_bsh

# Tribe or Khanate
flavor_bsh.1 = {
    type = country_event
    title = flavor_bsh.1.title
    desc = flavor_bsh.1.desc

    illustration_tags = {
        10 = regular
        10 = interior
    }
    fire_only_once = yes

    dynamic_historical_event = {
        tag = BKA
        from = 1444.1.1
        to = 1850.1.1
        monthly_chance = 100
    }

    trigger = {
        has_or_had_tag = BKA
    }

    immediate = {
        event_illustration_estate_effect = { foreground = estate_type:nobles_estate background = estate_type:nobles_estate }
    }

    option = {
        name = flavor_bsh.1.a

        add_legitimacy = legitimacy_extreme_bonus
        add_prestige = prestige_mild_bonus
    }

    option = {
        name = flavor_bsh.1.b

        add_prestige = prestige_mild_bonus
         change_government_type = government_type:steppe_horde
    }
}